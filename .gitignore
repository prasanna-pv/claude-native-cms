# secrets — never commit
.env
.env.*

# internal / private — do NOT publish (full manual: competitor teardown + Petavue battlecard)
context.private.md

# per-company artifacts, scraped ad data, debug dumps & generated pitch pages
data/
output/

# dependencies (restore with `npm install`)
node_modules/

# OS / editor noise
.DS_Store
*.log
